# OPEN-QUESTIONS.md

Непроверенные допущения. Каждый пункт закрывается проверкой на живом
инстансе или в исходниках, после чего результат переносится в `SPEC.md`,
а пункт отсюда удаляется.

**Ничего из этого файла нельзя считать фактом при написании кода.**

Файл существует отдельно от `SPEC.md` именно поэтому: смешивание
проверенного с предполагаемым приводит к тому, что догадка фиксируется
в тестах и становится «требованием».

---

# Блокирующие

## 1. Поведение `monitorNewItems` на идущих сериалах

**Что известно:** глобального значения по умолчанию для `monitorNewItems` нет,
задаётся только на сериал. Есть краевая проблема: эпизоды, дописанные позже
в уже отслеживаемый текущий сезон, могут оказаться немониторимыми.

**Что проверить:** заказать сезон **идущего** сериала и посмотреть, что
происходит с новыми эпизодами этого сезона и с началом следующего.

**Почему важно:** пользователь заказывает сезон, а не подписку на сериал.
Обратная ошибка — эпизод вышел, а Sonarr его не взял — тоже нежелательна.

**Стоп-условие №6** при обнаружении незаказанного мониторинга.

---

## 2. Стартует ли поиск фильма в prod-режиме

Прежняя формулировка касалась состава полей `POST /api/v3/movie`. **Эта часть
закрыта на этапе 4:** схема `openapi/radarr-v3-v6.3.0.10514.json` подтвердила
все поля из `SPEC.md` 2.4 — `tmdbId` (integer), `qualityProfileId` (integer),
`rootFolderPath` (string), `monitored` (boolean), `minimumAvailability`
(`MovieStatusType`), `addOptions` (`AddMovieOptions` с `searchForMovie`).

**Что осталось непроверенным:** что задача поиска действительно появляется в
Activity после добавления. В dev-режиме это принципиально не проверяется —
`searchForMovie` всегда `false`, и так задумано.

Здесь же живёт риск из `SPEC.md` 2.4: слишком строгое `minimumAvailability`
даёт «добавилось, но не ищет», причём **без ошибки**. Значение по умолчанию у
нас `released`.

**Как проверить:** только человеком, после подключения индексаторов и
осознанного переключения `BRIDGE_ENV=prod`. До тех пор пункт закрыт быть не
может.

**Стоп-условие №7** при обнаружении, что фильм добавлен, а поиск не стартовал.

---

# Неблокирующие

## 3. Prowlarr как парсер для Lampa

**Допущение:** Jackett-совместимый эндпоинт Prowlarr подходит под то, что
ожидает парсер Lampa, и один набор трекеров может обслуживать и *arr, и Lampa.

**Не проверялось.**

**Резервный путь:** JacRed для Lampa, Prowlarr для *arr. Два независимых
набора трекеров — некрасиво, но работает.

**Влияние:** только на второй поток (одноразовый просмотр). Основную цепочку
не задевает.

---

## 4. Категории qBittorrent в LME Torrent Manager

**Вопрос:** поддерживает ли плагин `torrentmanager.js` выбор категории или
каталога сохранения для конкретной загрузки в qBittorrent.

**Зачем:** если да — появляется альтернативный путь без bridge для части
сценариев. Скорее всего не нужен, но остаётся как факт для полноты.

**Как проверить:** посмотреть исходник `torrentmanager.js` на наличие полей
`category` или `savepath`, либо протестировать против живого qBittorrent.

**Влияние на текущую архитектуру:** нулевое. Выбор релиза руками из скоупа
исключён, заказ идёт через *arr.

---

## 5. Направление синхронизации Trakt-плагина Lampa

**Вопрос:** плагин `trakttv.js` пишет в Watchlist или только читает из него?

**Зачем:** если пишет — есть альтернативный мост «отметил в Lampa → Trakt →
импорт списка в Radarr/Sonarr», без кастомного плагина вообще, ценой задержки
на интервал синхронизации.

**Актуально только** если разработка плагина застопорится.

---

# Закрытые

Оставлено для истории, чтобы не проверять повторно.

- **Sonarr требует `tvdbId`, добавление по `tmdbId` не поддерживается.**
  Подтверждено. Трансляция через TMDB `/tv/{id}/external_ids` обязательна.
  Часть регионального контента `tvdb_id` не имеет.
- **Мониторинг одного сезона без будущих — возможно.** Через
  `monitorNewItems: "none"`. Прежнее ограничение снято.
- **Схемы OpenAPI не отдаются живым инстансом** по `/api/v3/openapi.json` на
  production-сборках; Swagger UI только в debug-режиме. Брать из исходников
  проектов.
- **Раздельные монтирования ломают жёсткие ссылки.** Подтверждено, это
  каноничная рекомендация TRaSH Guides.
- **`UMASK_SET` устарела**, использовать `UMASK`.
- **`VideoLibrary.Scan` с `directory` сломан для сериалов** начиная с Kodi
  v21 Omega. Для фильмов работает. Вне скоупа, но учтено в `SPEC.md`.
- **Лицензия Lampa** — ISC. Вопрос признан неважным, закрыт.
- **Как плагин получает `tmdb_id` и тип.** Подписка на `full`, обработка
  только `complite`; тип из `object.method`, данные в `data.movie`. Прочитано
  в исходниках `yumata/lampa-source`, ссылки на строки — в `SPEC.md` 3.2.
  Собранная `npm run doc` документация покрывает только функции с JSDoc,
  событий карточки в ней нет.
- **Плагин работает в живой Lampa.** Проверено 2026-08-01 на телефоне через
  Lampac: кнопка появляется в карточках фильма и сериала, выбор сезона и
  качества работает, уведомления показывают текст `detail`, в логах bridge
  правильные `tmdb_id` и тип. Этой же проверкой найдена гонка в мониторинге
  сезона, которую автоматика пропускала.
- **CORS нужен и в production.** Прежнее обоснование «плагин отдаётся с того
  же origin, что и API» неверно: CORS определяется origin страницы, а не
  скрипта. Проверено на живом стеке, подробности в `SPEC.md` 2.8.
- **Образ Lampac** — `ghcr.io/lampac-nextgen/lampac`, не `immisterio/lampac`:
  последнего на Docker Hub не существует. Без смонтированного `init.conf`
  сервис вяжется на `127.0.0.1` внутри контейнера и снаружи недоступен, при
  этом healthcheck показывает `healthy`.
- **Добавление сезона требует ровно двух шагов.** Проверено опытом на Sonarr
  4.0.19.2979 (этап 5). Один POST с массивом `seasons` и
  `addOptions.monitor: "none"` не работает: флаги затираются, а **ответ POST
  этого не показывает** — он возвращает `monitored: true`, тогда как в базе
  лежит `false`. Отдельно выяснилось, что `monitoringOptions` в `seasonpass` —
  такой же пресет-затирака: и `"none"`, и `"skip"` снимают мониторинг со всех
  сезонов, включая запрошенный. Объект не передаётся вовсе. Подробности с
  таблицами — в `SPEC.md` 2.5.
